<?xml version="1.0" encoding="UTF-8"?>
<persistence version="3.0"
             xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence
             https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd">

    <persistence-unit name="organizationPU" transaction-type="RESOURCE_LOCAL">
        <!-- Указываем провайдер -->
        <provider>org.eclipse.persistence.jpa.PersistenceProvider</provider>

        <!-- Мы НЕ указываем здесь jta-data-source -->

        <class>organization.entity.Organization</class>
        <class>organization.entity.Coordinates</class>
        <class>organization.entity.Address</class>
        <class>organization.entity.Location</class>
        <class>organization.entity.User</class>
        <class>organization.entity.ImportOperation</class>

        <shared-cache-mode>ENABLE_SELECTIVE</shared-cache-mode>

        <properties>
            <property name="jakarta.persistence.schema-generation.database.action" value="update"/>
            <property name="eclipselink.target-database" value="PostgreSQL"/>

            <!-- ВАЖНО: Добавляем параметры подключения прямо сюда, чтобы WildFly не падал при старте.
                 В коде EntityManagerProducer мы все равно подменим это на DBCP2 DataSource. -->
            <property name="jakarta.persistence.jdbc.driver" value="org.postgresql.Driver"/>
            <property name="jakarta.persistence.jdbc.url" value="jdbc:postgresql://localhost:5432/studs"/>
            <property name="jakarta.persistence.jdbc.user" value="s408391"/>
            <property name="jakarta.persistence.jdbc.password" value="1gaLfGH4bkg4oTgg"/>

            <property name="eclipselink.logging.level.sql" value="FINE"/>
            <property name="eclipselink.logging.parameters" value="true"/>
            <property name="eclipselink.cache.shared.default" value="true"/>
            <property name="eclipselink.cache.size.default" value="500"/>
        </properties>
    </persistence-unit>
</persistence>